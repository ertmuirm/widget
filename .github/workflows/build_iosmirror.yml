name: Build iOS IPA

on:
  workflow_dispatch:
  push:
    branches:
      - new

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: new
          fetch-depth: 1

      - name: Install xcodegen
        run: |
          which xcodegen || brew install xcodegen
          xcodegen --version

      - name: Generate Xcode Project
        run: |
          cd new
          xcodegen generate

      - name: Build (unsigned)
        run: |
          cd new
          xcodebuild archive -scheme IOSMirror -configuration Release -archivePath ./build/IOSMirror.xarchive CODE_SIGNING_ALLOWED=NO PRODUCT_BUNDLE_IDENTIFIER=com.iosmirror 2>&1 | tail -30

      - name: Package as IPA
        run: |
          cd new
          # Find the app bundle in the archive
          if [ -d "build/IOSMirror.xarchive/Products/Applications/IOSMirror.app" ]; then
            APP="build/IOSMirror.xarchive/Products/Applications/IOSMirror.app"
          elif [ -d "build/IOSMirror.xarchive/Products/Applications/Widget.app" ]; then
            APP="build/IOSMirror.xarchive/Products/Applications/Widget.app"
          else
            echo "App not found"
            find build -name "*.app" 2>/dev/null
            exit 1
          fi
          
          echo "Using: $APP"
          mkdir -p ../Payload
          cp -R "$APP" ../Payload/
          cd ../Payload
          APP_NAME=$(basename "$APP")
          chmod +x "$APP_NAME"/* 2>/dev/null || true
          zip -r ../IOSMirror.ipa "$APP_NAME"
          cd ..
          ls -la IOSMirror.ipa
          echo "IPA size: $(du -h IOSMirror.ipa | cut -f1)"

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: IOSMirror
          path: IOSMirror.ipa
          retention-days: 30